# Отчёт по задаче 10

Сделано:

## Часть А — патч v0.6
`..\multnews-patch-12.zip` распакован с заменой (`.env` не тронут): `config.yaml`, `docs/master-prompt-scenarist.md` (мастер-промпт v2 — рассказ без пауз, сцены как дословные куски), `tools/app.py` (архивация старых файлов при пересценарии — `_archive_outputs`), `tools/media.py` (`split_sentences`, `trim_silence`, `concat_audio`), `tools/render.py` (озвучка рассказа единым куском, границы сцен по таймингам слов), `tools/scenarist.py` (валидация под `narration`/`scenes[].text`), `tools/ui.html`, `tools/narrate.py` (новый — пробник голоса без пересборки), `CLAUDE.md`, этот файл.

Страница перезапущена в фоне (PID новый), оставлена работать: `/api/settings` вернула корректный JSON (`cta` уже новый: «А продолжение этой истории — в канале в шапке профиля»). Сервер голоса уже был жив с прошлой задачи: `GET /health` → `{"backend":"chatterbox","device":"cuda"}`, поднимать заново не понадобилось.

ID Grok: полный список моделей семейства Grok по `/v1/models` (не обрезанный топ-40 из `check_keys.py`):
```
grok-4.20-0309-non-reasoning
grok-4.20-0309-reasoning
grok-4.20-beta
grok-4.20-multi-agent-0309
grok-4.20-multi-agent-beta-0309
grok-4.3
grok-4.5
grok-4.6
grok-build-0.1
grok-code-fast
grok-code-fast-1
grok-imagine-image
grok-imagine-image-quality
```
Взял `grok-4.6` — это следующая по номеру версия после `grok-4.5` (примера из задачи) без каких-либо оговорок (не beta, не reasoning-сплит, не multi-agent, не датированный снапшот, не code/fast/build). Семейство `grok-4.20-*` новее по номеру, но КАЖДЫЙ вариант там с пометкой (`beta`, `-0309` дата, `reasoning`/`non-reasoning`, `multi-agent`) — трактовал это как превью/специализированные сборки, а не стабильный общий релиз, поэтому не взял. Прямой тестовый вызов `grok-4.6` через `llm.chat()` ответил корректно («Да»). Если у владельца есть точная информация о нумерации (вдруг 4.20 — это и есть текущий стабильный) — поправить одной строкой в `config.yaml`.

Коммит `v0.6: continuous narration, scene bounds by word timings, Grok scenarist, archive on re-script` — `5d4d580`, запушен. Генераций в части А не было.

## Часть Б — ролик по новому промпту
Текст новости взят из старой карточки `work\20260909-065554-ccmv\item.json → news.text` (384 символа, `news.source` — пустая строка, ссылки не было и в задаче 09). Новая карточка создана через `POST /api/items`: **id `20260910-103826-f993`**.

Сценарий (`POST .../script`) — `status: ok`, `_validation: []` (сошлось с первого раза, доработка не понадобилась). Заголовок: «Пилот закурил — лайнер камнем вниз».

Объект `hero`:
- name: Амит Шарма
- gender: male, age: 44
- look_en: South Asian man, short black hair greying at the temples, thick dark eyebrows, round face, stocky medium build, slightly heavy eyelids
- outfit_en: white airline captain shirt with gold-striped epaulettes, black necktie, dark navy trousers, shiny black shoes

`narration` целиком (63 слова):
> Что случилось на рейсе Пхукет — Дели? Командир судна перед взлётом стоял в тени и курил траву. Глаза поплыли, а он уже сел за штурвал. Самолёт оторвался — и капитан потерял контроль. Лайнер резко пошёл вниз, люди взлетели с кресел. Двадцать пассажиров и четверо экипажа в крови. Катастрофу остановил второй пилот в одиночку. А продолжение этой истории — в канале в шапке профиля

8 сцен, текст каждой — дословный кусок рассказа по порядку (проверено валидатором: `_validation` пуст, «текст сцен не совпадает дословно» не сработало).

### Сборка
`POST .../approve {variants:1}` → сразу запустила рендер, на этот раз без коллизий (карточка новая, файлов от прошлых задач нет). Прошла с первого раза, без ошибок.

Ход `busy` (опрос раз в 20 с; переходы, сек от старта):
```
10:41:21  портрет героя: front
10:41:41  портрет героя: full_body
10:42:02  вариант 1: кадры 1/8
10:43:03  вариант 1: кадры 2/8
10:45:05  вариант 1: кадры 3/8
10:47:27  вариант 1: кадры 4/8
10:47:48  вариант 1: кадры 5/8
10:50:10  вариант 1: кадры 6/8
10:50:30  вариант 1: кадры 7/8
10:50:51  вариант 1: кадры 8/8
10:51:11  озвучка 1/8
10:51:31  озвучка 4/8
10:51:52  озвучка 8/8
10:52:12  вариант 1: оживление идёт
10:53:13  вариант 1: субтитры и сборка
10:53:54  busy:null   status:done   error: (пусто)
```
Итого 12,5 минуты, без единой ошибки. `total_sec: 25.27` — внутри целевого окна 18–28 с для новости среднего размера (у неё три поворота: закурил → потерял управление → второй пилот спас).

Лист кадров и портреты открыты и просмотрены: узнаваемый герой (седеющие виски, круглое лицо) на всех 8 кадрах, сцена 2 буквально показывает курение с дымом (ключевая деталь новости — по правилу мастер-промпта v2 «главная сочная деталь обязана быть в кадре хотя бы в двух сценах»; здесь видна явно в сцене 2, состояние «поплыли глаза» читается в сценах 2–3), сцена 6 — паника пассажиров, сцена 8 — посадка. Стиль тёплый, насыщенный, сцены живые и наполненные людьми, как требует `style/style.md`.

Тайминги сцен из `render.json` (start / scene_sec, секунды):
| сцена | start | scene_sec |
|---|---|---|
| 1 | 0.00 | 2.98 |
| 2 | 2.98 | 3.22 |
| 3 | 6.20 | 2.70 |
| 4 | 8.90 | 3.30 |
| 5 | 12.20 | 3.52 |
| 6 | 15.72 | 2.84 |
| 7 | 18.56 | 0.80 |
| 8 | 19.36 | 5.91 |

`total_sec: 25.27`, сборка заняла 709 с (≈12 мин, `render.json → seconds`).

## Сравнение голосов (`tools/narrate.py`, бесплатно+копейки)
`--provider aimlapi --voice Decent_Boy --speed 1.1` → `preview_aimlapi_Decent_Boy.wav`, 24.1 с.
`--provider aimlapi --voice Young_Knight --speed 1.1` → `preview_aimlapi_Young_Knight.wav`, 23.0 с.
Оба звучат тем же текстом рассказа (по предложениям, без ролика). Для сравнения — локальный клон в самом ролике: `voice/narration.wav`, 25.3 с.

## Файлы в multnews-reports/renders/
- `pilot_v3.mp4` — 1080×1920, h264/aac, 25.27 с, 21,5 МБ.
- `pilot_v3_scenes.png` — лист 8 кадров.
- `pilot_v3_hero.png` — портрет героя (анфас).
- `pilot_v3_voice_clone.wav` — озвучка рассказа локальным клоном (та, что в ролике).
- `preview_aimlapi_Decent_Boy.wav`, `preview_aimlapi_Young_Knight.wav` — пробники для сравнения.

Коммит: `5d4d580` (v0.6); отчёт — следующим коммитом в оба репозитория.

Ошибки: нет.

Вопросы:
1. Выбор `grok-4.6` вместо семейства `grok-4.20-*` — решение по косвенным признакам (см. часть А), не стопроцентно уверен в схеме нумерации Grok. Если знаете точно — поправить в `config.yaml → models.apiyi.text` одной строкой.
2. Сцена 7 вышла очень короткой (0.8 с) — сработал предохранитель `MIN_SCENE` в `render.py`, когда два соседних слова разошлись по времени теснее полсекунды. Кадр не мелькает фатально, но заметно короче остальных на глаз — не трогал код, просто отмечаю для сведения.